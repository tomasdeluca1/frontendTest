const UserSearched = ({ login, id, node_id }) => {
  return (
    <div>
      <h1>Login: {login}</h1>
      <h1>Id: {id}</h1>
      <h1>Node_Id: {node_id}</h1>
    </div>
  );
};

export default UserSearched;
